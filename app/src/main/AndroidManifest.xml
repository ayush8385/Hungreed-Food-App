<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ayush.hungreed">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hungreed">
        <activity android:name=".activity.Final"></activity>
        <activity android:name=".activity.Cart" />
        <activity android:name=".activity.FoodItems" />
        <activity android:name=".activity.Dashboard" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.Chnage_pass" />
        <activity android:name=".activity.Forgot" />
        <activity android:name=".activity.Signup" />
        <activity android:name=".activity.Login" />
        <activity android:name=".activity.user" />
        <activity android:name=".activity.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>